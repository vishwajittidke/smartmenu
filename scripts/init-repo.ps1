# Initialize git repository, commit, and create main branch
git init
git add .
git commit -m "chore: initial SmartMenu scaffolding"
git branch -M main
echo "Repository initialized. Add remote and push as needed."
