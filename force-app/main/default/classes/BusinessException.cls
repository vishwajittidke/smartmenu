public with sharing class BusinessException extends Exception {
    // Custom exception class for domain-level business rule violations.
    // Ensures consistent error handling across the service and domain layers.
}
